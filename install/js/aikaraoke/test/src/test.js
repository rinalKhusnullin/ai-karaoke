import {Type} from 'main.core';

export class Test
{
	constructor(options = {name: 'Test'})
	{
		this.name = options.name;
	}

	setName(name)
	{
		if (Type.isString(name))
		{
			this.name = name;
		}
	}

	getName()
	{
		return this.name;
	}
}
